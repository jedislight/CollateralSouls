event_inherited()
opt = "ON"
if(Config.reals[?"show_alerts"] == false)
{
    opt = "OFF"
}
text = "Show Enemy Alerts ["+opt+"]"
detail = "If ON visual alert will be shown when a new enemy becomes visible"

