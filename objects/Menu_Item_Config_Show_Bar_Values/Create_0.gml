event_inherited()
opt = "ON"
if(Config.reals[?"show_bar_values"] == false)
{
    opt = "OFF"
}
text = "Show Exact Values ["+opt+"]"
detail = "If ON HP/XP bar will show exact values as well as the bar"

