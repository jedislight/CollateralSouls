event_inherited()
opt = "ON"
if(Config.reals[?"collapse_event"] == false)
{
    opt = "OFF"
}
text = "Collapse Event ["+opt+"]"
detail = "If OFF the world collapse event will be disabled at the end of cleared floors"

