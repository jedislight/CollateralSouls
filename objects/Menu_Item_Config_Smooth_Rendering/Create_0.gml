event_inherited()
opt = "ON"
if(Config.reals[?"Smooth_Rendering"] == false)
{
    opt = "OFF"
}
text = "Smooth Rendering ["+opt+"]"
detail = "Adjusts blending method for inter-pixel rendering. Different settings will look better on diffirent displays."

